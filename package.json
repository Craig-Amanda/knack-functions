{
    "name": "knack-functions",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "npm run build:global",
        "build:global": "mkdir -p dist && cp knackFunctions.js dist/knackFunctions.global.js && terser dist/knackFunctions.global.js -o dist/knackFunctions.global.min.js --compress --mangle",
        "dev": "node server.js",
        "start": "npm run dev",
        "simple": "node simple-server.js"
    },
    "devDependencies": {
        "terser": "^5.34.0",
        "express": "4.18.2",
        "cors": "2.8.5",
        "chokidar": "3.5.3"
    }
}
